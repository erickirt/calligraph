---
"calligraph": patch
---

Polish character animations: 
- stagger characters by position
- faster exit transitions
- add polymorphic `as` prop
- add automatic `aria-label` on container
- add `drift` object with `x` and `y` axes
- add `stagger` prop to control cascade timing
- add `initial` prop to animate on first mount
- add `onComplete` callback
