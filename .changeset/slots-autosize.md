---
"calligraph": minor
---

Add `"slots"` variant for slot-machine style digit transitions and `autoSize` prop that animates wrapper width to match content. Refactor key reconciliation and shared utilities into dedicated modules.
